(hissp.macros.._macro_.prelude)
.#"from lisscad.shorthand import *"

;(define model1
;  (->> (cube '(1 2 4))
;    (translate '(0 0 8)))
;
;(define model2
;  (->> (square '(1 2))
;    (translate '(0 4))
;    (union)))
;
;(define assets
;     (dict : name "model1" model model1)
;     (dict : name "model2" model model2))

(lisscad.app..write (@ (dict : name "bliku" model (cube '(1 2 4)))))
